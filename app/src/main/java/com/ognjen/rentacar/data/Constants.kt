package com.ognjen.rentacar.data

class Constants {
    companion object {
        const val BASE_URL = "localhost:8080/api/v1/"

        const val LOGIN_URL = "auth/login"
        const val REGISTER_URL = "auth/register"
        const val REFRESH_URL = "auth/refresh"

        const val USERS_URL = "users"

        const val INVOICES_URL = "invoices"

        const val CATEGORIES_URL = "categories"

        const val PRODUCTS_URL = "products"

    }
}